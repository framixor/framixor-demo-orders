// src/tenants/bloom-oven/theme.ts

import type { ThemeContract } from "../../core/tenant/tenant.types";

export const bloomOvenTheme = {
  colors: {
    background: "#FAF7F0",
    foreground: "#2F2A24",
    primary: "#8FAE8B",
    primaryForeground: "#FFFFFF",
    muted: "#EFE8DA",
  },
} satisfies ThemeContract;